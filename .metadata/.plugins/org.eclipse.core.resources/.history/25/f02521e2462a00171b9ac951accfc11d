package com.gui.product;

import com.library.Utilities;
import com.object.*;

import java.text.*;
import java.util.*;

public class ProductLibrary {

	public static String viewProductForCondition(ArrayList<ProductObject> items,int condition){
		String tmp = "";
		
		switch (condition) {
		case 1:
			
			break;

		default:
			break;
		}
		
		return tmp;
	}
	public static String viewProduct(ArrayList<ProductObject> items){
		String tmp = "";
		for(ProductObject item : items){
			tmp += "<div class=\"product\">";
			tmp += "<div class=\"product-code\">Mã sản phẩm: LT001</div>";
			tmp += "<div class=\"product-image\">";
			tmp += "<a href=\"\" title=\"\"><img src=\"\" alt=\"\"></a>";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "";
			tmp += "</div><!-- end Product-->";
		}
		
		return tmp;
	}
}
